## Checkpoints

The checkpoint files are managed by Git LFS. During the anonymous review period, the anonymized repository may display LFS pointer files depending on the anonymous hosting service. The full checkpoint files will be released after acceptance.
